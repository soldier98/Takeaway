#=====================生产环境==============
spring:
  config:
    activate:
      on-profile: prod
server:
  port: 8080
#spring:
#  web:
#    resources:
#      static-locations: classpath:/static/ #配置静态资源路径，默认访问static下index.html
#  main:
---
spring:
  main:
    banner-mode: off #关闭springBoot启动图案
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:13306/takeaway?useSSL=false
    username: root
    password: abc123
    type: com.alibaba.druid.pool.DruidDataSource
  redis:
    host: 139.196.104.225
    port: 7759
    password: C150mmon
  cache:
    redis:
      time-to-live: 3600000 #设置过期时间
mybatis-plus:
  configuration:
    #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映射
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: ASSIGN_ID
    banner: off
---
takeaway:
  path: /usr/local/TakeawayPicture/
spring:
  shardingsphere:
    datasource:
      names:
        master,slave
      # 主数据源
      master:
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://139.196.104.225:13306/takeaway?useSSL=false
        username: root
        password: abc123
      # 从数据源
      slave:
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:13306/takeaway?useSSL=false
        username: root
        password: abc123
    masterslave:
      # 读写分离配置
      load-balance-algorithm-type: round_robin #轮询
      # 最终的数据源名称
      name: dataSource
      # 主库数据源名称
      master-data-source-name: master
      # 从库数据源名称列表，多个逗号分隔
      slave-data-source-names: slave
    props:
      sql:
        show: true #开启SQL显示，默认false
  main:
    allow-bean-definition-overriding: true #允许后定义的bean覆盖之前定义的